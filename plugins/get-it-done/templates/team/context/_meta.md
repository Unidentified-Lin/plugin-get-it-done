# Project Meta

_Identifying information for this project. The first agent to touch this file fills in any missing fields; later agents only update `Last cycle`._

## Project ID
(auto-derived from working directory basename on first agent run, e.g. `flowchart-app`)

## Working directory
(absolute path of the project root, recorded on first agent run)

## First touched
(ISO timestamp of the first agent run after `/objective`)

## Last cycle
(ISO timestamp of the most recent agent run)

## One-line description
(short summary written by Planner during the first PLANNING phase, e.g. "Browser-based flowchart editor with PNG export")

## Why this file exists

Agents read `_meta.md` first when picking up an in-progress project. It tells them: "yes, this is the project you remember; here's what it is." It's also the anchor that the other B-side learning files attach to.
