# Validation Log

_Append-only verdicts from Validator. The `/continue` dispatcher truncates this in place when the file exceeds 500 lines (keeps last 250)._

_(empty — populated by Validator on each VALIDATING cycle)_
